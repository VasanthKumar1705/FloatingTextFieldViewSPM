import XCTest
@testable import FloatingTextFieldViewSPM

final class FloatingTextFieldViewSPMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FloatingTextFieldViewSPM().text, "Hello, World!")
    }
}
